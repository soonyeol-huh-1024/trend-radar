# 자격증명 — .env.example 을 복사해 값을 채운다
.env

# 파이썬
venv/
__pycache__/
*.pyc

# macOS
.DS_Store

# 수집 데이터 — Firestore/BigQuery 에서 다시 만들 수 있고 용량이 크다(64MB)
#   python fetch_candidates.py 30
#   python fetch_amazon_bq.py
data/

# 코너 추출 결과 — run_radar.py 가 매주 새로 만든다
out/
magazine/out_email/

# 빌드 산출물 — build_issue0N.py 로 템플릿에서 재생성된다
magazine/sellerkim_issue*.html

# 네이버쇼핑 상품 이미지는 판매자 저작물이라 재배포하지 않는다.
# 매거진 빌드에 필요하면 ItemScout API 로 다시 받는다.
magazine/img/nv_*.jpg

*.xlsx

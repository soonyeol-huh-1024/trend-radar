# 트렌드레이더 — 유료 상품 설계

셀러KIM은 **모객**이고, 트렌드레이더가 **파는 것**이다. (2026-09-21 확정)

---

## 왜 필요한가

매거진은 매주 4,678개 키워드를 계산해 놓고 **코너당 5~10개만** 싣는다.
나머지 99%는 아무 데도 나가지 않는다. 꼬리말에 이미 적어 둔 약속이 그것이다 —
*"2027년부터는 기사 뒤에 있는 원본 데이터만 따로 내놓을 계획입니다."*

그런데 4호를 만들면서 더 중요한 걸 알았다.

> 3호에 **치이카와 앉은누이 등록 상품 48개**라고 썼다.
> 2주 뒤 다시 세어 보니 **908개**였다. 검색은 4%밖에 안 올랐는데 상품만 19배.

이 발견은 **사람이 손으로** 했다. 매거진은 이걸 독자마다 해 줄 수 없다.
독자마다 보는 키워드가 다르기 때문이다.

**트렌드레이더가 파는 것은 데이터가 아니라 이 관찰이다.**
"내가 지켜보는 자리가 지금 채워지는 중인가"를 매주 대신 봐 주는 것.

---

## 무엇을 만드는가

### 첫 버전 — 워치리스트

1. 셀러가 키워드를 등록한다 (자기 상품, 노리는 자리, 경쟁 키워드)
2. 매주 각 키워드의 **한 달 검색 · 등록 상품 수 · 검색÷상품**을 기록한다
3. 지난주 대비 변화를 계산해 **신호**로 바꾼다

| 신호 | 조건 | 뜻 |
|---|---|---|
| 🔴 **밀려난다** | 상품 +20% 이상 **이면서** 검색 -10% 이하 | 공급은 늘고 수요는 식는다 — 가장 나쁜 조합 |
| 🟠 **자리가 채워지는 중** | 상품 +30% 이상, 검색 변화 15% 미만 | 경쟁자가 들어오고 있다 (치이카와 사례) |
| 🟠 **검색이 빠진다** | 검색 -25% 이하 | 수요가 식는다 |
| 🟢 **자리가 열린다** | 검색 +30% 이상, 상품 +15% 미만 | 수요가 공급보다 먼저 왔다 |
| 🔵 **곧 온다** | 계절 예보상 2~8주 뒤 정점 | 준비 구간 (아직 미구현) |
| ⚪ 조용함 | 위 어디에도 안 걸림 | 메일에 안 보낸다 |

판정 순서가 중요하다. '밀려난다'를 맨 앞에 두지 않으면 **상품이 늘면서 검색이 빠지는**
가장 나쁜 경우가 어느 규칙에도 안 걸려 조용히 넘어간다 — 실제로 오디세이책(상품 +34%,
검색 -19%)이 그랬고, 실측으로 돌려 보고서야 찾았다.

4. 매주 월요일 메일 한 통: 걸린 신호만. 조용한 키워드는 안 보낸다.

### 두 번째 — 전체 탐색기

매거진 코너의 **원본 전체**를 검색·필터·정렬한다.
계절 예보 420개, 주간 키워드 4,678개, 크로스보더 갭 전체.
워치리스트에 담는 입구 역할도 한다.

### 안 하는 것 (처음에는)

- 틱톡→아마존 전이 확률 같은 예측 모델 (`Trend_Arbitrage_SaaS_Plan.md` 의 ②③④)
  — 백테스팅 데이터가 아직 얇다. 관찰이 먼저다.
- 소싱·발주 조언. 매거진에서 빼기로 한 것을 상품에 넣지 않는다.

---

## 어디에 올리는가

**새 웹앱** (Next.js + Cloud Run + Firebase Auth + Firestore).
기존 `dashboard/` 와 같은 스택이지만 **별도 앱**이다 —
대시보드는 투자(종목 추천) 쪽이고 독자층이 다르다. 한 화면에 섞으면 둘 다 흐려진다.

## 과금

**2027년부터.** 그때까지 무료 베타로 열어 두고 쓰는 사람이 뭘 원하는지 본다.
매거진 꼬리말에 이미 그렇게 적혀 있으므로 약속을 지킨다.

---

## 데이터

이미 있는 것만으로 첫 버전이 된다.

| 필요한 것 | 어디서 |
|---|---|
| 한 달 검색 · 등록 상품 수 | ItemScout API (`radar/common.py`) |
| 주간 키워드 4,678개 | Firestore `weekly_keywords/{주}` |
| 계절 예보(2~8주 뒤 정점) | `radar/seasonal.py` — 최대 471주 시계열 |
| 크로스보더 갭 | `radar/bridge_corners.py` + `amazon_weekly` |
| 주차별 이력 | **새로 쌓는다** (아래) |

### 이력은 왜 새로 쌓나

지금 파이프라인은 매주 새로 계산만 하고 **키워드별 과거를 남기지 않는다.**
그래서 "2주 만에 19배"를 사람이 손으로 찾아야 했다.

전체 4,678개를 매주 스냅샷하면 무겁고 대부분 안 쓴다.
**워치리스트에 담긴 키워드만** 쌓는다. 사용자가 늘어도 중복은 한 번만 조회한다.

```
radar_watch/{uid}              등록한 키워드 목록
radar_history/{키워드}/{주}     monthly · prd_cnt · ratio · 조회시각
```

---

## 만드는 순서

1. ~~**이력·신호 엔진** (`radar/watch.py`)~~ ✅ 2026-09-21 완료
   — `snapshot`(적재) · `signals`(판정) · `add`(등록). 3호 지면 숫자를 이력으로 넣고 돌려
   치이카와(자리가 채워지는 중)와 오디세이책(밀려난다)을 자동으로 잡는 것까지 확인
2. 주간 배치 — 월요일 자동 실행, 신호 계산
3. 메일 발송 — 걸린 신호만
4. 웹 UI — 로그인 · 키워드 등록 · 신호 목록
5. 전체 탐색기

1~3 은 UI 없이도 돌아간다. 매거진 팔로업 코너도 이걸 쓰면 손으로 안 찾아도 된다.
